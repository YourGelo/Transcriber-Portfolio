# Local demo values only. Services are not published on external interfaces.
POSTGRES_USER=demo
POSTGRES_PASSWORD=local-demo-password
POSTGRES_DB=transcriber
S3_ACCESS_KEY=local-demo-user
S3_SECRET_KEY=local-demo-password
S3_BUCKET=audio
WHISPER_MODEL=tiny
